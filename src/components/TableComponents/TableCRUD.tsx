import { type FC } from "react";

type Props = {
  update: () => void;
  delete: () => void;
  id: number;
};
export const TableCRUD: FC<Props> = ({update,id}) => {
    return(
        <>

        </>
    )
};
